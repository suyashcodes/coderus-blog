import React from 'react'

export default function Loading() {
  return (
    <div>
    {/* to see the loading effect goto inspect in browser slow down the network and see the loading content will occur */}
      Loading
    </div>
  )
}
